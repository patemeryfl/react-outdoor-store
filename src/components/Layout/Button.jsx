import styled from 'styled-components';
import colors from '../../style/colors';

const Button = styled.button`
    background-color: ${colors.firstLight};
    border: 1px solid ${colors.dark};
    border-radius: 5px;
    padding: .5em;
    margin-top: -10px;
`
export default Button;